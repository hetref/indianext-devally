{
    "name": "ai-api",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "node --watch src/index.ts",
        "build": "echo \"Build pipeline not configured yet for ai-api\"",
        "lint": "echo \"Lint not configured yet for ai-api\"",
        "check-types": "tsc --noEmit"
    },
    "devDependencies": {
        "typescript": "^5"
    }
}